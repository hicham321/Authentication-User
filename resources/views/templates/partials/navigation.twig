    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only"> Toggle navigation</span>
                    <span class="icon-bar">Toggle navigation</span>
                    <span class="icon-bar">Toggle navigation</span>
                    <span class="icon-bar">Toggle navigation</span>
                </button>
                <a class="navbar-brand" href="#">{{ trans('messages.website') }}</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li><a href="{{path_for('home')}}">{{ trans('messages.home') }}</a> </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {% if auth.check %}
                    <li><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ auth.user.userName }} <span class="caret"></span> </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{path_for('auth.changePass')}}">{{ trans('messages.changePassword') }} </a> </li>
                        <li><a href="{{path_for('auth.signOut')}}">{{ trans('messages.signout') }} </a> </li>
                    </ul>
                    </li>
                    {% else %}
                     <li><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">lang<span class="caret"></span> </a>
                    <ul class="dropdown-menu">
                        <li><a href="#" value="{{lang.en}}">english</a> </li>
                        <li><a href="#" value="{{lang.fr}}">french</a> </li>
                    </ul>
                    </li>
                    <li><a href="{{ path_for('auth.signup')}}"> {{ trans('messages.signup') }} </a> </li>
                    <li><a href="{{ path_for('auth.signin')}}"> {{ trans('messages.signin') }} </a> </li>
                    {% endif %}
                </ul>
            </div>
        </div>

    </nav>﻿