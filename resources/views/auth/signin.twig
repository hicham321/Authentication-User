{% extends 'templates/app.twig' %}

{% block content %}

<div class="row">
	
    <div class="col-md-6 col-md-offset-3">
    	<div class="panel panel-default">
    		<div class="panel-heading">  		
    		   {{ trans('messages.signin') }}
    		</div>
    		<div class="panel-body">
    			<form action="{{path_for('auth.signin')}}" method="post">
    				<div class="form-group {{ errors.email ? 'has-error' : ''}}" >
    					<label for="email">{{ trans('messages.email') }} </label>
    					<input type="email" name="email" id="email" placeholder="{{ trans('messages.emailDomain') }}" class="form-control" value="{{ old.email}}">
                        <span class="help-block">
                            {% if errors.email %}

                             {{errors.email | first}}
                            {% endif %}
                        </span>
    				</div>
    				<div class="form-group {{ errors.password ? 'has-error' : ''}}">
    					<label for="password">{{ trans('messages.password') }}</label>
    					<input type="password" name="password" id="password"  class="form-control" value="{{ old.name}}">
                         <span class="help-block">
                            {% if errors.password %}

                             {{errors.password | first}}
                            {% endif %}
                        </span>
    				</div>

    				<button type="submit" class="btn btn-danger"> {{ trans('messages.signin') }} </button>

                    {{ csrf.field | raw}}
                    

    			</form>
    		</div>
    	</div>
    </div>
	
</div>

{% endblock %}